# Frontend/finance-dashboard/src/auth/

- 認証方式は Cognito Managed Login + PKCE
- トークンは sessionStorage に保存する（localStorage 不可）
- トークン読み取りは `getIdToken()`, `getAccessToken()` 経由で行う
- `import.meta.env.DEV` 時は Cognito 通信を行わずモックトークンを使用する
