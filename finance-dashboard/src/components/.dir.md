# Frontend/finance-dashboard/src/components/

- ファイル名は PascalCase（例: `AppHeader.vue`）
- `<script setup lang="ts">` を使用する
- UI ライブラリは PrimeVue 4 のみ。Bootstrap は使用しない
- 新規コンポーネント追加時は `Frontend/docs/component_design.md` を更新すること
