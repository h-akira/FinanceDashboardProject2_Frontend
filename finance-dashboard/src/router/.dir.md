# Frontend/finance-dashboard/src/router/

- ルーティング設計は `Frontend/docs/routing.md` を参照
- 認証が必要なルートには `meta: { requiresAuth: true }` を設定する
- 認証状態の検査は `router.beforeEach` で行う
- 新規ルート追加時は `Frontend/docs/routing.md` も更新すること
