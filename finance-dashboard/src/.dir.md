# Frontend/finance-dashboard/src/

- コーディング規約・技術スタックは `Frontend/docs/technical_policies.md` に従う
- ページコンポーネントは `pages/` に、再利用コンポーネントは `components/` に配置する
- API の型は Orval 自動生成（`api/generated/`）を使い、手動定義しない
- 外部ライブラリの型定義は `types/` に配置する
- `any` 禁止。やむを得ない場合はコメントで理由を記載する
